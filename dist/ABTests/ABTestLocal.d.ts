import React from "react";
import { ABTestLocalProps } from "./types";
export declare const ABTestLocal: {
    ({ children, name, testRef, localStorage }: ABTestLocalProps): JSX.Element | null;
    Variant: {
        ({ children, name }: {
            children: React.ReactNode;
            name: string;
        }): React.FunctionComponentElement<any>;
        displayName: string;
    };
};
